[pytest]
asyncio_mode = auto
testpaths = qa
addopts = --tb=short

markers =
    slow: marks tests as slow (>5s). Run with: pytest -m slow
    integration: tests that require live backend
    e2e: full end-to-end tests
    unit: fast unit tests (no external services)
